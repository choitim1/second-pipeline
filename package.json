{
  "name": "cicd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "runOnChrome": "cypress run --browser chrome",
    "kirill" :"npx cypress run --spec cypress/e2e/kiriil_koel.cy.js",
    "spec" : "npx cypress run --spec cypress/e2e/spec.cy.js"
   },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^11.1.0"
  }
}
